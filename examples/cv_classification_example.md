# CV Classification Demo

This module is a lightweight CV baseline for JD alignment.

```bash
python scripts/run_cv_classification_demo.py --config configs/cv_classification_demo.yaml
```

Set `download: true` if you want torchvision to download CIFAR10. The demo is not the main multimodal evaluation pipeline.
